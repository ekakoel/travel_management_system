/**
 * Backend entry point.
 *
 * During the migration period this bundle wraps the existing legacy
 * backend application entry so we can introduce a dedicated backend
 * build output without breaking current behavior.
 */

import '../../js/app';
